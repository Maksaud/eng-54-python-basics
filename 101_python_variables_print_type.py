# Variables
# It is like a box. You give it a name, put stuff inside
# defining variable book
book = 'Rich dad poor dad'

# Print function
# outputs content to the terminal (makes it visible)
book
print(book)

# type Function
# allows us to check datatypes
data_type_of_book = type(book)
print(data_type_of_book)

# input() - prompt for user input
# nd you can add a string to the argument so it prints that before capturing the input
# example
input('This gets printed before capturing your input')


## Convention
# lower case for variable naming with underscore
